#!/usr/bin/make -f

export DH_VERBOSE=1

%:
	dh $@ 

override_dh_auto_build:
	sudo npm install
	$(CURDIR)/node_modules/.bin/bower install
	$(CURDIR)/node_modules/.bin/gulp

override_dh_install:
	dh_install build/ /usr/share/guh-webinterface/public
